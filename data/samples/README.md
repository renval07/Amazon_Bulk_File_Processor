# Sample Data

This folder is intended for local sample/input files used for testing.

By default, `.xlsx` files are gitignored to keep the repository lightweight and avoid committing sensitive account data.
