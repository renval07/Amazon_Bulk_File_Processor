# Legacy Test Scripts

This folder stores the original script-style test runners.

These are kept for historical reference only. Active automated testing now lives in the `tests/` pytest suite.

Run active tests with:

```bash
python -m pytest tests/
```
